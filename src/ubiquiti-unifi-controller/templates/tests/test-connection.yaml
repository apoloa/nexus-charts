apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ubiquiti-unifi-controller.fullname" . }}-test-connection"
  labels:
    {{- include "ubiquiti-unifi-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ubiquiti-unifi-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
